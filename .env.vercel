# Vercel Environment Variables
# Copy these to your Vercel Dashboard -> Project Settings -> Environment Variables

NODE_ENV=production
MONGODB_URI=mongodb+srv://chodagiriprasad5:Prasad%40123@cluster0.w4dod.mongodb.net/meat-delivery?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=your-super-secret-jwt-key-here-please-change-in-production
JWT_EXPIRE=7d

# Make sure to set these in Vercel Dashboard for security:
# - MONGODB_URI (your MongoDB Atlas connection string)
# - JWT_SECRET (a strong secret key for JWT tokens)
# - Any other sensitive environment variables